version: '3.9'
services:
  claw-wasteland-api:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - DATABASE_URL=file:./claw_wasteland.db
